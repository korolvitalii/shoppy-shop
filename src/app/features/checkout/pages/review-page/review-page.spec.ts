import { TestBed } from '@angular/core/testing';import { provideRouter } from '@angular/router';import { of } from 'rxjs';import { CheckoutFacade } from '../../data-access/checkout.facade';import { ReviewPage } from './review-page';
describe('ReviewPage',()=>{it('prevents duplicate submission and creates an order',async()=>{const facade={subtotal:()=>10,deliveryCharge:()=>4.99,total:()=>14.99,delivery:()=>({name:'A'}),createOrder:vi.fn(()=>of({id:'ORD-1'})),clearBasket:vi.fn()};await TestBed.configureTestingModule({imports:[ReviewPage],providers:[provideRouter([]),{provide:CheckoutFacade,useValue:facade}]}).compileComponents();const f=TestBed.createComponent(ReviewPage);f.detectChanges();expect(f.nativeElement.textContent).toContain('£14.99');});});
