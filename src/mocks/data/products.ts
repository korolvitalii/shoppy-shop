import catalogue from '../../app/features/catalogue/data/catalogue.json';
import { type Product } from '../../app/features/catalogue/models/product';

export const mockProducts: readonly Product[] = catalogue.products;
